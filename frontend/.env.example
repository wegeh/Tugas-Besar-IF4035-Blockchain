# === Database & Auth (WAJIB) ===
# Ganti user, password, dan nama database sesuai PostgreSQL lokal Anda
DATABASE_URL="postgresql://postgres:password@localhost:5432/carbon_db?schema=public"

# Ganti dengan string acak apa saja (minimal 32 karakter) untuk keamanan login
# Digunakan oleh NextAuth.js
NEXTAUTH_SECRET="874315745AC1A66C4718168C22EDA33414BF916EB9C2981834212CADD6"
NEXTAUTH_URL="http://localhost:3000"

# === Blockchain Configuration (OPSIONAL) ===
# URL RPC ke Node Blockchain (Geth)
NEXT_PUBLIC_RPC_URL="http://127.0.0.1:8545"

# Relayer Private Key (Fallback jika node account terkunci/tidak ada)
# Default Makefile akan membuka kunci node otomatis, jadi ini opsional.
RELAYER_PRIVATE_KEY="0x6C1D21A692A4a5e711172FB1D921e6fC6A2108e5"

# Catatan: ENV dibawah ini TIDAK PERLU diisi manual jika menggunakan 'make bootstrap'.
# Makefile akan menyuntikkannya secara otomatis saat seeding.
# - VALIDATOR (Wallet Address Validator)
# - RELAYER (Wallet Address Relayer)
NEXT_PUBLIC_IPFS_API_URL=
NEXT_PUBLIC_IPFS_GATEWAY_URL=
NEXTAUTH_URL=
AUTH_URL=
AUTH_TRUST_HOST=